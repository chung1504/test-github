**Hiểu nhanh**

- git clone + link repository: tải project lần đầu tiên vào máy, giống như tải về lần đầu
- git pull: + cập nhật thêm code mới trong project đã có sẵn, giống như cập nhật về sau + git pull = git fetch + git merge: nó lấy code về mà merge vào code hiện tại
